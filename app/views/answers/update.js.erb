<% if !answer.errors.present? %>
  $('.best_answer').html('<%= j render 'best_answer' %>');
  $('.answers').html('<%= j render @question.other_answers %>');
<% end %>

$('<%= ".answer-#{answer.id}-errors" %>').html('<%= render 'shared/errors', resource: answer %>');